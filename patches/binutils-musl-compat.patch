--- a/binutils/readelf.c
+++ b/binutils/readelf.c
@@ -371,7 +371,7 @@ enum versioned_symbol_info
 static int
 fseek64 (FILE *stream, int64_t offset, int whence)
 {
-#if defined (HAVE_FSEEKO64)
+#if defined (HAVE_FSEEKO64) && defined (__GLIBC__)
   off64_t o = offset;
   if (o != offset)
     {
